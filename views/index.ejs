<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Edu+VIC+WA+NT+Beginner&display=swap" rel="stylesheet">
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div class="contenedorLocal">
      <header class="header">
        <h1 class="titulo">Acortador de Enlaces Free</h1>
      </header>

      <div class="formulario">
        <form method="post" action="/shorturl" class="row g-2">
          <div class="input-group input-group-lg">
            <input id="url" name="url" type="url" class="form-control" placeholder="Ingrese el enlace Aqui" aria-describedby="btnAcortar">
            <button id="btnAcortar" class="btn btn-primary" type="submit">Acortar</button>
          </div>
        </form>
      </div>
      
      <div class="contenedor">
        <% if(shortUrl != "") { %>
          <div class="row alert alert-success" role="alert">
            <a class="text-center enlaceShort" href="<%=shortUrl%>" target="_blank"  ><%=shortUrl%></a>
          </div>
        <% } %>
      </div> 

      <div class="contenido">
        <table id="tabla" class="table table-info table-striped">
          <thead>
          <tr>
            <th>#</th>
            <th>Enlace Original</th>
            <th>Enlace Short</th>
          </tr>
          
          <tbody id="tbody">
            <% for(let enlace in Enlaces ) { %> 
              <tr>
                <td> <%=Enlaces[enlace].id_link %> </td>
                <td> <%=Enlaces[enlace].enlaceOriginal %> </td>
                <td> <a href="http://localhost:3000/url<%=Enlaces[enlace].enlaceShort %>" target="_blank" ><%=Enlaces[enlace].enlaceShort %></a> </td>
              </tr>
            <% } %>   
      
          </tbody>
        </table>

      </div> 

    </div>
   
  </body>
</html>
